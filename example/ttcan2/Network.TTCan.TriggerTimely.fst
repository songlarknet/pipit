(** Simplified trigger fetch + timeliness proof.

   NOT yet ported: this module is heavily dependent on
   [Pipit.Sugar.Check] / [Pipit.Sugar.Contract] (which no longer exist)
   and on multi-arm matches over stream scrutinees / [pre] / static
   `cfg` parameters. Stubbed for now. *)
module Network.TTCan.TriggerTimely
